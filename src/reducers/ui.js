import {
    ROUTE_TO
} from '../data/ActionTypes'

const defaultState = {
}

const ui = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default ui